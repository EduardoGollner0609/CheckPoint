
export type RegisterCompanyDTO = {
    name: string;
    document: string;
    email: string;
    password: string;
};

export type RegisterUserDTO = {
    name: string;
    document: string;
    email: string;
    password: string;
    code: string;
};
